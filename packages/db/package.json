{
  "name": "@propagent/db",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "migrate": "ts-node src/migrate.ts",
    "migrate:auth": "ts-node src/auth-migration.ts",
    "migrate:worker": "ts-node src/worker-migration.ts",
    "migrate:analytics": "ts-node src/analytics-migration.ts",
    "migrate:billing": "ts-node src/billing-migration.ts",
    "seed": "ts-node src/seed.ts"
  },
  "dependencies": {
    "pg": "^8.11.3",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "@types/pg": "^8.10.9"
  }
}
